services:
  iris:
    image: intersystemsdc/iris-community:latest
    container_name: iris-estudos
    ports:
      - "52773:52773"
      - "1972:1972"
    environment:
      - IRIS_PASSWORD=demo
      - IRIS_USERNAME=demo
    volumes:
      - ./iris-data:/dur
    restart: unless-stopped